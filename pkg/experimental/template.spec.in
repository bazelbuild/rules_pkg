# -*- rpm-spec -*-

# Configurable compression
# This is a variable "definedness" test
%if "0%{?build_rpm_binary_payload}" != "0"
  %define _binary_payload %{build_rpm_binary_payload}
%endif

# This comprises the entirety of the preamble
%include %build_rpm_options

%description
%include %build_rpm_description

%install
%include %build_rpm_install

%files -f %build_rpm_files

${PRE_SCRIPTLET}

${POST_SCRIPTLET}

${PREUN_SCRIPTLET}

${POSTUN_SCRIPTLET}
